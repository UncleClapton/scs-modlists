# TODO

## Tracker Evidence Lists

- Return structured evidence item arrays from the Rust achievement analysis JSON instead of only compact display strings.
- Keep `AchievementEvidence.value` for short inline summaries, but add a separate field for full pop-out contents.
- Use the structured field for long lists such as completed cargoes and Reliable Contractor companies so summary markers like `+103 more` are never treated as real list items.
